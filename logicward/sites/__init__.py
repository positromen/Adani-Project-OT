"""Monitored sites beyond the core thermal plant.

Each site is a self-contained process + Modbus surface that feeds the same
LogicWard event bus / MITRE map / evidence contract. `grfics` is the GRFICS v3
chemical reactor with its real Unity 3D visualization (Site B).
"""
